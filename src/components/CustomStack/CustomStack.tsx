import { StackProps } from '@mui/material';
import { CustomStackStyled } from './CustomStackStyled.styles';

export const CustomStack: React.FC<StackProps> = () => {
  return <CustomStackStyled />;
};
